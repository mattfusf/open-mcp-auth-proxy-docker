# Environment variables for docker-compose

# Proxy Configuration
PROXY_PORT=8080
PROXY_MODE=demo  # Options: demo, asgardeo, stdio

# MCP Server Configuration
MCP_SERVER_PORT=8000

# Asgardeo Configuration (when using --asgardeo mode)
# Uncomment and set these when using your own Asgardeo organization
# ASGARDEO_ORG_NAME=your-org-name
# ASGARDEO_CLIENT_ID=your-client-id
# ASGARDEO_CLIENT_SECRET=your-client-secret

# Auth0 / Keycloak Configuration (when using custom OAuth provider)
# Update config.yaml with your provider's details

# Debug Mode
# DEBUG=true
